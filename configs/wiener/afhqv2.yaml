defaults:
  - /defaults.yaml

experiment:
  run_name: wiener_afhqv2
  tags: [baseline, wiener, afhqv2]

dataset:
  name: afhqv2
  split: train
  download: true
  batch_size: 256
  subset_size: null
  resolution: 64

model:
  name: wiener
  params:
    use_precomputed_pca: true  # download published PCA instead of computing covariance
