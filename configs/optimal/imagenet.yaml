defaults:
  - /defaults.yaml

experiment:
  run_name: optimal_imagenet
  tags: [baseline, optimal, imagenet]

dataset:
  name: imagenet
  split: train
  download: true
  batch_size: 256
  subset_size: null
  resolution: 64

model:
  name: optimal
  params:
    temperature: 1.0
    num_neighbors: 200
