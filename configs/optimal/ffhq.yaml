defaults:
  - /defaults.yaml

experiment:
  run_name: optimal_ffhq
  tags: [baseline, optimal, ffhq]

dataset:
  name: ffhq
  split: train
  download: true
  batch_size: 256
  subset_size: null
  resolution: 64

model:
  name: optimal
  params:
    temperature: 1.0
    num_neighbors: 200
