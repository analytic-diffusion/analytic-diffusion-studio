defaults:
  - /defaults.yaml
  - /comparison/_base_ffhq.yaml

# pretrained EDM UNet (learned reference)
experiment:
  run_name: cmp_ffhq_edm_unet
  tags: [comparison, ffhq, edm_unet, heun]

model:
  name: edm_unet
  params:
    use_fp16: false
